name: "Quality Assurance"

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  qa:
    name: "✅ Quality Assurance"
    uses: ./.github/workflows/qa.general.workflow.yaml

    with:
      node-version: ${{ matrix.node-version }}
      pnpm-version: 8

    strategy:
      matrix:
        node-version: [18, 19]

  cleanup:
    name: "🧹 Cleanup"
    runs-on: ubuntu-latest
    if: always()
    needs:
      - qa

    steps:
      - name: "Delete artifacts"
        uses: geekyeggo/delete-artifact@v2
        with:
          name: workspace-*
