{
  "name": "@kayak/example-cypress-plugin",
  "version": "0.0.0",
  "license": "MIT",
  "author": "Evgeniy Kozlov",
  "scripts": {
    "cypress": "env-cmd -f .env cypress run --browser chromium --headed",
    "lint": "eslint --no-fix --ext .js,.ts .",
    "serve": "serve --single --no-request-logging -p 3000 ./src",
    "test": "start-server-and-test serve http://localhost:3000 cypress"
  },
  "devDependencies": {
    "@kayak/cli": "workspace:*",
    "@kayak/cypress": "workspace:*",
    "@kayak/eslint-config": "workspace:*",
    "cypress": "^12.9.0",
    "env-cmd": "^10.1.0",
    "eslint": "^8.37.0",
    "serve": "^14.2.0",
    "start-server-and-test": "^2.0.0",
    "typescript": "^5.0.3"
  }
}
